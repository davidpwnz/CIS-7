/*- [ ] Write a program that contains 2 sets (Set A, and Set B).  You can usewhatever underlying typeyou want to represent these sets, but they should hold string values- [ ] In the program loop, the user should be displayed the values of thecontents of A and B aswell as a menu with the following options:        - Add a value to A: Allow the user to add a string value to A        - Add a value to B: Allow the user to add a string value to B        - A interset B: Prints out the contents of A interset B        - A union B: Prints out the contents of A union B        - A diff B: Prints out the contents of A - B        - B diff A: Prints out the contents of B - Add        - Powersets: Prints out the powersets of A and B- [ ] Upload your assignment along with the dankest meme you can find to GitHub- [ ] Email james.wilson@rccd.edu with the url to the source code on GitHub*/
#include <iostream>#include <string>using namespace std;
int main() {  string setA, setB;  int list;  string item;  bool stop = false, stop2 = false;  char resp;  do {    cout << "1. Add item to A" << endl         << "2. Add item to B" << endl         << "3. AuB" << endl         << "4. A^B" << endl         << "5. A-B" << endl         << "6. B-A" << endl         << "7. AxB" << endl         << "8. Powerset A" << endl         << "9. Powerset B" << endl         << endl;    cout << "Enter your numerial option: ";    cin >> list;    do {      switch (list) {      case 1:        cout << "Enter item for set A: ";        cin >> item;        if (setA == "")          setA = item;        else          setA = setA + ", " + item;        break;      case 2:        break;      case 3:        break;      case 4:        break;      case 5:        break;      case 6:        break;      case 7:        break;      case 8:        break;      case 9:        break;      default:        return 1;      };      cout << "Continue with additional iteration of current selection? (y/n)"           << endl;      cin >> resp;      if (resp == 'n')        stop = true;    } while (!stop);    cout << "Make new selection? (y/n)" << endl;    cin >> resp;    if (resp == 'n')      stop2 = true;  } while (!stop2);  cout << "{ " << setA.length() << " }" << endl << "{ " << setB << " }" << endl;
}}
